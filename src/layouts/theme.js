import { theme } from '@nextui-org/react';

const customTheme = {
    ...theme,
    colors: {
        ...theme.colors,
        secondary: '#2d9fae',  // Cambia el color "secondary"
    },
};

export default customTheme;
